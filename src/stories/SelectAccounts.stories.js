import {SelectAccounts} from '../components/SelectAccounts';

const SelectAccountsMeta = {
  title: 'SelectAccounts',
  component: SelectAccounts,
};

export default SelectAccountsMeta;

export const Default = {
  args: {
    setShowSelectAccounts: () => {},
  },
};
