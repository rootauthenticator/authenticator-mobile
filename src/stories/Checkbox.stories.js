import {Checkbox} from '../components/Checkbox';

const CheckboxMeta = {
  title: 'Checkbox',
  component: Checkbox,
};

export default CheckboxMeta;

export const Default = {
  args: {
    onPress: () => {},
    isChecked: true,
  },
};
