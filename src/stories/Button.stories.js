import {Button} from '../components/Button';

const ButtonMeta = {
  title: 'Button',
  component: Button,
};

export default ButtonMeta;

export const Default = {
  args: {
    title: 'Button',
  },
};
